import React from 'react';
import { Grid } from 'react-bootstrap';

import './footer.scss';

export default () => (
    <footer id="screen-footer">
        <Grid>
            This is a footer
        </Grid>
    </footer>
);
