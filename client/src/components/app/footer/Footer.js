import React from 'react';
import { Grid } from 'react-bootstrap';

import './footer.scss';

export default () => (
    <footer className="screen-footer">
        <Grid>
            Footer
        </Grid>
    </footer>
);
