import React from 'react';

import AuthLayout from './';

const conf = {
  title: 'auth/AuthLayout',
};

export default conf;

export const Basic = () => (
  <AuthLayout>
    <></>
  </AuthLayout>
);
